package org.iesra

class LogProcessor (private val parser : LogParser = LogParser(), private val filter : LogFilter = LogFilter(), private val stats : LogStats = LogStats()){
    fun ejecutarProceso(lineasBrutas : List<String>){

    }
}